{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "d1_value_signal_v1.schema.json",
  "title": "D1ValueSignalBundleV1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "forecast_run_id",
    "model_version",
    "rules_version",
    "station",
    "city",
    "weather_date_local",
    "weather_metric",
    "data_status",
    "weather_bundle_sha256",
    "candidates"
  ],
  "properties": {
    "forecast_run_id": { "type": "string", "minLength": 1 },
    "model_version": { "type": "string", "const": "D1_1500" },
    "rules_version": { "type": "string", "const": "D1_manual_v1.0" },
    "station": { "type": "string", "enum": ["ZSPD", "ZBAA"] },
    "city": { "type": "string", "minLength": 1 },
    "weather_date_local": { "type": "string", "pattern": "^\\d{4}-\\d{2}-\\d{2}$" },
    "weather_metric": { "type": "string", "enum": ["highest_temperature", "high", "highest", "最高温", "最高气温"] },
    "data_status": {
      "type": "string",
      "enum": ["COMPLETE", "PARTIAL", "CONFLICTING", "STALE", "LEAKAGE_INVALID"]
    },
    "weather_bundle_sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
    "candidates": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": true,
        "required": [
          "temperature_bucket",
          "forecast_probability",
          "market_slug",
          "condition_id",
          "token_id",
          "outcome",
          "market_ask_price",
          "edge",
          "recommended_max_price",
          "intended_usd",
          "reason",
          "data_status",
          "orderbook_snapshot_id",
          "orderbook_snapshot_sha256",
          "orderbook_captured_at_utc"
        ],
        "properties": {
          "forecast_run_id": { "type": "string" },
          "station": { "type": "string", "enum": ["ZSPD", "ZBAA"] },
          "weather_date_local": { "type": "string" },
          "weather_metric": { "type": "string" },
          "temperature_bucket": { "type": "string" },
          "forecast_probability": { "type": "number", "minimum": 0, "maximum": 1 },
          "market_slug": { "type": "string", "minLength": 1 },
          "condition_id": { "type": "string", "minLength": 1 },
          "token_id": { "type": "string", "minLength": 1 },
          "outcome": { "type": "string", "minLength": 1 },
          "market_ask_price": { "type": "number", "minimum": 0, "maximum": 1 },
          "edge": { "type": "number" },
          "recommended_max_price": { "type": "number", "minimum": 0, "maximum": 1 },
          "intended_usd": { "type": "number", "exclusiveMinimum": 0 },
          "reason": { "type": "string" },
          "data_status": {
            "type": "string",
            "enum": ["COMPLETE", "PARTIAL", "CONFLICTING", "STALE", "LEAKAGE_INVALID"]
          },
          "orderbook_snapshot_id": { "type": "string", "minLength": 1 },
          "orderbook_snapshot_sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
          "orderbook_captured_at_utc": { "type": "string", "minLength": 1 },
          "orderbook_snapshot_evidence_path": { "type": "string" }
        }
      }
    }
  }
}
